<div class="after-nav py-5 alert alert-info text-center d-flex justify-content-center align-items-center" role="alert" style="height: 60px;">
  <strong>Preview: </strong> This is a preview of the article.
</div>

<div class="container article-container after-nav py-5">
  <div class="row">
    <div class="col-md-8">
      <!-- Article Header -->
      <div class="row">
        <div class="col-12">
          <h2><%= @article.title %></h2>
          <p class="text-muted">
            By <span class="fw-bold"><%= @article.author.first_name+ ' ' +@article.author.last_name  %></span> • Last Updated on <span><%= @article.formatted_updated_at  %></span>
          </p>
        </div>
      </div>
      <!-- Article Image -->
      <div class="row">
        <div class="col-12">
          <%= image_tag @article_image_url, alt: "Article Image", class: "img-fluid rounded mb-4" %>
        </div>
      </div>
      <!-- Tags -->
      <div class="row">
        <div class="col-12 mb-3">
          <% @article.tags.each_with_index do |tag, idx|%>
            <span class="badge <%= @tag_classes[idx] %>" ><%= tag.name.capitalize  %></span>
          <% end %>
        </div>
      </div>
      <div class="row border-bottom">
        <p class="fst-italic fw-semibold"><%= @article.description %></p>
      </div>
      <!-- Article Body -->
      <div class="row">
        <div class="col-12">
          <%= @article_body %>
        </div>
      </div>
    </div>
    <div class="col-md-4">
    </div>
    <div class="row">
      <div class="container mt-5">
        <!-- Share Button -->
        <div class="d-flex align-items-center">
          <i class="fas fa-share-alt fa-2x me-3"></i>
          <!-- Social Media Icons -->
          <div>
            <a href="https://www.facebook.com/sharer/sharer.php?u=#{CGI.escape(article_url(@article))}" class="text-decoration-none text-primary me-3">
              <i class="fab fa-facebook fa-lg"></i>
            </a>
            <a href="https://twitter.com/intent/tweet?url=#{CGI.escape(article_url(@article))}&text=#{CGI.escape(@article.title)}" class="text-decoration-none text-dark me-3">
              <i class="fab fa-x-twitter fa-lg"></i>
            </a>
            <a href="#" class="text-decoration-none text-danger me-3">
              <i class="fab fa-instagram fa-lg"></i>
            </a>
            <a href="https://www.linkedin.com/sharing/share-offsite/?url=#{CGI.escape(article_url(@article))}" class="text-decoration-none text-secondary">
              <i class="fab fa-linkedin fa-lg"></i>
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

























